---
import CompanyCard from './CompanyCard.astro';
import { site } from '../data/site';
import { getGroupedCompanies, getEnglishById, getCardProps } from '../lib/companies';
import { withBase } from '../lib/url';

interface Props {
  locale: 'tr' | 'en';
}
const { locale } = Astro.props;

const { hotEnd, coldEnd, ceramics } = await getGroupedCompanies();
const enById = await getEnglishById();

const wassmerCard = {
  href: site.wassmer.officialUrl,
  logo: site.wassmer.logo,
  name: site.wassmer.name,
  blurb: site.wassmer.summary[locale],
  external: true,
};
---

<section class="hero">
  <div class="container">
    <h1>{site.hero.heading[locale]}</h1>
    <p>{site.hero.subheading[locale]}</p>
  </div>
</section>

<section id="glass" class="section">
  <div class="container">
    <div class="section-header">
      <h2>{site.about.glass.title[locale]}</h2>
      <p>{site.about.glass.body[locale]}</p>
    </div>

    <h3 class="segment-heading">{site.glassSegments['hot-end'][locale]}</h3>
    <div class="card-grid">
      {
        hotEnd.map((entry) => {
          const card = getCardProps(entry, locale, enById);
          return (
            <CompanyCard
              href={card.href}
              logo={entry.data.logo}
              name={entry.data.name}
              blurb={card.blurb}
              external={card.external}
            />
          );
        })
      }
    </div>

    <h3 class="segment-heading">{site.glassSegments['cold-end'][locale]}</h3>
    <div class="card-grid">
      {
        coldEnd.map((entry) => {
          const card = getCardProps(entry, locale, enById);
          return (
            <CompanyCard
              href={card.href}
              logo={entry.data.logo}
              name={entry.data.name}
              blurb={card.blurb}
              external={card.external}
            />
          );
        })
      }
    </div>
  </div>
</section>

<section id="ceramics" class="section section-alt">
  <div class="container">
    <div class="section-header">
      <h2>{site.about.ceramics.title[locale]}</h2>
      <p>{site.about.ceramics.body[locale]}</p>
    </div>

    <div class="card-grid">
      {
        ceramics.slice(0, 4).map((entry) => {
          const card = getCardProps(entry, locale, enById);
          return (
            <CompanyCard
              href={card.href}
              logo={entry.data.logo}
              name={entry.data.name}
              blurb={card.blurb}
              external={card.external}
            />
          );
        })
      }
      <CompanyCard
        href={wassmerCard.href}
        logo={wassmerCard.logo}
        name={wassmerCard.name}
        blurb={wassmerCard.blurb}
        external={wassmerCard.external}
      />
      {
        ceramics.slice(4).map((entry) => {
          const card = getCardProps(entry, locale, enById);
          return (
            <CompanyCard
              href={card.href}
              logo={entry.data.logo}
              name={entry.data.name}
              blurb={card.blurb}
              external={card.external}
            />
          );
        })
      }
    </div>
  </div>
</section>

<section id="contact" class="section">
  <div class="container">
    <div class="section-header">
      <h2>{site.contact.title[locale]}</h2>
    </div>
    <div class="contact-grid">
      <div class="map-block">
        <a
          class="map-card"
          href={site.contact.directionsUrl}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={site.contact.labels.directions[locale]}
        >
          <img src={withBase(site.contact.mapImage)} alt="Geçit" loading="lazy" />
          <span class="map-card-cta">{site.contact.labels.directions[locale]} ↗</span>
        </a>
        <a class="map-attribution" href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener noreferrer">
          {site.contact.mapAttribution[locale]}
        </a>
      </div>
      <div class="contact-details">
        <div class="row">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 1 1 16 0Z" /><circle cx="12" cy="10" r="3" />
          </svg>
          <span>{site.contact.addressLines.join(', ')}</span>
        </div>
        <div class="row">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path
              d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.362 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.338 1.85.573 2.81.7A2 2 0 0 1 22 16.92Z"
            />
          </svg>
          <span>{site.contact.labels.phone[locale]}: {site.contact.phone}</span>
        </div>
        <div class="row">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path
              d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.362 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.338 1.85.573 2.81.7A2 2 0 0 1 22 16.92Z"
            />
          </svg>
          <span>{site.contact.labels.fax[locale]}: {site.contact.fax}</span>
        </div>
        <div class="row">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="2" y="4" width="20" height="16" rx="2" /><path d="m22 6-10 7L2 6" />
          </svg>
          <a href={`mailto:${site.contact.email}`}>{site.contact.email}</a>
        </div>
      </div>
    </div>
  </div>
</section>
